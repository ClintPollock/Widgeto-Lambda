name: Test Britive OIDC Login

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  test-login:
    runs-on: ubuntu-latest

    steps:
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install pybritive
      run: pip install pybritive --quiet

    - name: Federated Britive Checkout
      run: |
        echo "🔐 Federated checkout using GitHub OIDC + env var"
        pybritive --version
        mkdir /home/runner/.britive/
        pybritive configure tenant --tenant se-demo --no-prompt
        ls -la /home/runner/.britive/
        pybritive checkout "AWS Standalone/Widgeto Prod/Widgeto-App-S3-Prod-Access" \
          --federation-provider github \
          --mode json
      env:
        PYBRITIVE_TENANT: se-learn
